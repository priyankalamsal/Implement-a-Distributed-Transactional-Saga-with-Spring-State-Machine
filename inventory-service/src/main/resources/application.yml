spring:
  application:
    name: inventory-service

server:
  port: ${SERVER_PORT:8082}

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      probes:
        enabled: true

inventory:
  failing-order-id: ${FAILING_INVENTORY_ORDER_ID:302}
